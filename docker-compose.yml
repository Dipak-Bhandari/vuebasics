services:
  vuebasics:
    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: vuebasics

    ports:
      - '5173:5173'

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      - CHOKIDAR_USEPOLLING=true
